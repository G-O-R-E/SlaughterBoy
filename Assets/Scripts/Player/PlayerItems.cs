using UnityEngine;

public class PlayerItems : MonoBehaviour
{
    [SerializeField] GameObject[] prefabsItems;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
